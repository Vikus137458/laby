#include <stdio.h>
#include <math.h>
using namespace std;

int main ()
{
        int  x ;
        printf (" Enter x { 2.0 < x } ") ;
        scanf ("% d " , & x ) ;
        if ( x > 2 )
        {
                            int y = - log (x + 2) - log (x - 2);
                            printf ("% d" , &y) ;
        }

        /*else
                {
                        double y = 0;
                        printf (" x value is incorrect !\ n ") ;
                }
        printf (" Enter y { y < -sqrt (3) & 0 < y < sqrt(3) } -> ") ;
        /*if ((y < - sqrt (3)) || (y > 0 && y < sqrt(3)))
                double z = sqrt (3y - y * y * y)
        else
                printf (" y value is incorrect !\ n ") ;
        printf (" y ( x ) = % lf \ nz ( y ) = % lf \ n " , y , z ) ;
        */return 0;
}
